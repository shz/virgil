import sub.b

export function foobar {
  let b = new Bojangles

  1 + 1
}

method doSometing(m : Magic<'T>) {
  # Or, actually, do nothing!
}

function test {
  let m = new Magic<int>
  m.doSomething()
}
