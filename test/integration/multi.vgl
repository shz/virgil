import foo/bar
export {
  a = add
  b = foo
}

# This is a comment

function add(a : int, b : int) returns int {

}

function add(int a, int b) returns int {
  return a + b
}

function sum(list<int> myList) returns int {


  return 0 # Yep
}

function iHaveNoArguments {
  return void
}

let a = "This is a string"
out b = "This is a string"
mut c = "This is a string"
c = "And the variable remains mutable"

let f = 1.0
let ff : float = 1
let fff = 1f

let powah = 2 ** 4

let p : perc = f

# let width = 10px

# let t = 100ms

let i = add(1, add(2, 3))

# Not yet supported
let l : list<int> = [1, 2, 3, 4]
let ll = [
  1,
  2,
]

function renderSomething(canvas c, data d, clock cl) {
  c.fillStyle = 'rgba(1, 1, 1)';
  c.fillRect(0, 0, 10, 10)
}

function(canvas, data, clock) {
  c.delay(500).fade(100, 200).drawThing();
  c.translate({x: 10, y: 10}, 100).
}

struct Shazam {
  a = 1
  b : int = add(1, 2)
}

let h = Hash<str, int>()
h.set("foo", 1)
h.get("foo")

let a : bool = true

method aFunction(Shazam s) returns int {
  return s.a
}

let s = Shazam()
s.aFunction()

mut test = true
while test {
  console.log("inside loop")
  test = false
}

let a = [1, 2, 3]
a.sum()

let sh = Shazam()
add(sh.a, sh.a)

if true {
  c = "True!"
} else {
  c = "False!"
}

function main(canvas c, data d) {
  c.drawRect(0, 0, 10, 10)


}

let a =
