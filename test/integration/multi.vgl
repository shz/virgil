# This is a comment

function add(a : int, b : int) returns int {
  return a + b
}

function sum(myList : list<int>) returns int {


  return 0 # Yep
}

function iHaveNoArguments {
  return void
}

let a = "This is a string"
out b = "This is a string"
mut c = "This is a string"
c = "And the variable remains mutable"

let f = 1.0
let ff : float = 1
let fff = 1f

let powah = 2 ** 4

let p : perc = f

# let width = 10px

# let t = 100ms

let i = add(1, add(2, 3))

# Not yet supported
let l : list<int> = [1, 2, 3, 4]
let ll = [
  1,
  2,
]

struct Shazam {
  a = 1
  b : int = add(1, 2)
}

let a : bool = true

let s = Shazam()
s.aFunction()

mut test = true
while test {
  console.log("inside loop")
  test = false
}

let a = [1, 2, 3]
a.sum()

let sh = Shazam()
add(sh.a, sh.a)

if true {
  c = "True!"
} else {
  c = "False!"
}

