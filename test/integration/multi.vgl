# This is a comment

function add (int a, int b) returns int {
  return a + b
}

function sum (list<int> a) returns int {
  return 0 # Yep
}

function iHaveNoArguments {
  return void
}

let a = "This is a string"
out b = "This is a string"
mut c = "This is a string"
c = "And the variable remains mutable"

let f = 1.0
let ff : float = 1
let fff : float = 1.0f

let p : perc = f

let i = add(1, add(2, 3))

# Not yet supported
let l : list<int> = [1, 2, 3, 4]
let ll = [
  1,
  2,
]

struct shazam {
  struct nested {
    a = "hi"
  }

  a = 1
  b : int = nested()
}

let sh = shazam()
add(sh.a, sh.a)

if true {
  c = "True!"
} else {
  c = "False!"
}

